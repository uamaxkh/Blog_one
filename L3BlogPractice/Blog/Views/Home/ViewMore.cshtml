@{
    @model DBLib.Models.Article
}
@*<div class="marginTop panel panel-primary">
    <div class="panel-heading text-uppercase">@Model.Name</div>
    <div class="panel-body">
        @Model.Text
    </div>
    <div class="panel-footer">
        @foreach (var tag in Model.Tags)
        {
            <span class="tag">@tag.Name</span>
        }
        <span> | </span> <span>@Model.Date.ToString("yyyy/MM/dd")</span>
    </div>
</div>*@

<h2 class="noMarginBottom">@Model.Name</h2>
<p class="dateSection">@Model.Date.ToString("yyyy/MM/dd")</p>
<div>@Model.Text</div>
<div class="tagSection">
    @foreach (var tag in Model.Tags)
    {
        <span class="tag">@tag.Name</span>
    }
</div>

<div><a href="/Home/Index">Повернутися на голову</a></div>